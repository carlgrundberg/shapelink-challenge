Shapelink Challenge
===================

# Requirements
* mongobb running on localhost

# Setup

Add a config file in the root folder.

```
{
  "shapelink": {
    "apiKey": "YOUR_API_KEY",
    "secret": "YOUR_SECRET"
  }
}
```

# Start server with

npm start

Default port is 3000

